<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts-config PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 1.1//EN"
                               "http://jakarta.apache.org/struts/dtds/struts-config_1_1.dtd">

<struts-config>

    <!-- Mappages d'action -->
    <!-- @todo Gère les erreurs -->
	<global-forwards>
		<forward name="failure" path="/error.do"></forward>
		<forward name="exception" path="/error.do"></forward>
		<forward name="error" path="/error.do"></forward>
	</global-forwards>
	
    <action-mappings>
	    <action path="/Error" parameter="errorpage" type="org.apache.struts.actions.ForwardAction" />

	    <action path="/Index" parameter="homepage" type="org.apache.struts.actions.ForwardAction"/>
	    
	    <action path="/AlreadyWeaved" parameter="alreadyweaved" type="org.jmonitoring.sample.console.SampleAlreadyWeavedAction">
	      	<forward name="success" path="/Ok.do"/>
		</action>

	    <action path="/Ok" parameter="ok" type="org.apache.struts.actions.ForwardAction"/>

	    
    </action-mappings>

	<plug-in className="org.apache.struts.tiles.TilesPlugin">
        <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml"/>
    </plug-in>

	<plug-in className="net.sf.navigator.menu.MenuPlugIn">
  		<set-property property="menuConfig" value="/WEB-INF/menu-config.xml"/>
	</plug-in>
</struts-config>
